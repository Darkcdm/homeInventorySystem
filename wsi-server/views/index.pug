extends layout

block content
  h1= title
  p Welcome to #[em !{title}].

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The WSI has the following record counts:

    ul
      li #[strong Items:] !{data.item_count}
      li #[strong Location:] !{data.location_count}

    button(type="button" onclick="window.location.href='/catalog/location/create'") #[strong Create root location]